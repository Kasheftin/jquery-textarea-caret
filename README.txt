Here're couple of dummy methods for working mostly with textareas.
They're formatted as jquery-plugin.
$("textarea").insertAtCursor("some text"); // inserts "some text" at the cursor-specified position.
$("textarea").insertAroundCursor("before text","after text"); // inserts "before text" before the selection and "after text" - after it.

